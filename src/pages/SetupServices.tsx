import { useQuery } from "@tanstack/react-query"

export default function SetupServices() {

    

    return (
        <div>
            <label htmlFor="">Tus servicios: </label>
            <ul>
                <li>Servicios del barbero</li>
            </ul>
        </div>
    )
}